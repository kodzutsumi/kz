import type { $UseFinal } from './$use_final.ts';

/**
 * Specifies that finalization should be a tied (`boolean`) for the {@linkcode $UseFinal} capability.
 */
export type $AsTie = $UseFinal<boolean>;
