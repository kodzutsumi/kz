import type { $UseReversed } from './$use_reversed.ts';

/**
 * Specifies that reversed order is enabled for the {@linkcode $UseReversed} capability.
 */
export type $AsReversed = $UseReversed<true>;
