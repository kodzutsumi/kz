import type { $UseReversed } from './$use_reversed.ts';

/**
 * Specifies that reversed order is disabled for the {@linkcode $UseReversed} capability.
 */
export type $AsForward = $UseReversed<false>;
